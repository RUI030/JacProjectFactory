[project]
name = "factory"
version = "0.1.0"
description = "Factory stage: spec -> gated build -> bucket"
entry-point = "main.jac"
kind = "cli"
jac-version = "==0.36.0"

[dependencies]

[dev-dependencies]
watchdog = ">=3.0.0"
