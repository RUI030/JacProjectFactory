[project]
name = "customer"
version = "0.1.0"
description = "Customer stage: persona -> wish -> spec"
entry-point = "main.jac"
kind = "cli"
jac-version = "==0.36.0"

[dependencies]

[dev-dependencies]
watchdog = ">=3.0.0"
